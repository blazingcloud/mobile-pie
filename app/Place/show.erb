<div class="pageTitle">
  <h1><%= @place.name:string %></h1>
</div>

<div class="toolbar">
  <div class="leftItem backButton">
    <a href="<%= url_for :action => :index %>">Back</a>
  </div>
  <div class="rightItem regularButton">
    <a href="<%= url_for :action => :edit, :id => @place.object %>">Edit</a>
  </div>
</div>

<div class="content">
  <ul>
    
        <li>
          <div class="itemLabel">Name:string</div>
          <div class="itemValue"><%= @place.name:string %></div>
        </li>
    
  </ul>
</div>